<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="home.css">
    <script src="home.js"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <body>
    <div class="banner">
      <h1>Welcome to ShipIT</h1>
      <% if current_user %>
        <p>Hello <%= current_user.email %>, you are a <%= current_user.role %>.</p>
      <% end %>
    </div>
    <div class="container">
      <div class="main">
        <div class="content">
          <h2>Fast and Reliable Shipping Service</h2>
          <p>ShipIT offers a fast, reliable, and secure shipping service for your items.</p>
        </div>
        <div class="cta">
          <% if current_user %>
            <%= link_to "Ship Now", new_order_path, class: "btn btn-lg btn-primary" %>
          <% else %>
            <%= link_to "Get Started", new_user_registration_path, class: "btn btn-lg btn-primary" %>
          <% end %>
        </div>
      </div>
      <footer class="footer mt-auto py-3">
        <div class="container">
          <span class="text-muted">&copy; 2023 ShipIT</span>
        </div>
      </footer>
    </div>
  </body>
</html>

