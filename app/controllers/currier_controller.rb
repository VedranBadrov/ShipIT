class CurrierController < ApplicationController
    #def pending_orders
    #    @orders = Order.where(user_id: nil)
   # end

   # def accept_order
  #      order = Order.find(params[:id])
  #      order.update(user_id: current_user.id, order_status: "accepted")
  #      redirect_to root_path, notice: "Order accepted!"
  #  end

  #  def accepted_orders
  #      @orders = Order.where(user_id: current_user.id, order_status: "Accepted")
   #   end
end
